/
  # Корневая папка, в ней только файлы
  .gitignore
  README.md
  PipFile

/doc
  # документация по проекту

/game <= 
  # игровой движок: python
  
/server
  # ядро http сервера: python
  /core <= /server/server
  /storage
    /models
  
/clients
  # различные клиенты
  /console
    # простой консольный клиент, работает напрямую с ядром: python
  /windows
    # оконный клиент, работает через http-сервер: delphi
  /web
    # браузерный клиент, работает через http-сервер
  /mobile
    # мобильный клиент, работает через http-сервер
    